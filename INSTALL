lolbot, not even ready for a version number, wtf.

you need google protocol buffers and its python bindings.

cd into the working directory and do

$>protoc --python_out=DST_DIR Mumble.proto

now you should see a file named Mumble_pb2.py

everything you need to do is edit main.py and lol.

kthxbai

hannes++
